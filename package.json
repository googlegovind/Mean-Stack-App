{
  "name": "mean-simple-seed",
  "version": "1.0.0",
  "description": "MEAN (MongoDB + node.js + Express + AngularJS) single-page multi-view app (MongoDB can be replaced w/ other db)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gokul",
  "license": "",
  "dependencies": {
    "express": "^4.10.0",
    "mongoose" : "~3.8.0",
    "socket.io" :"*"
  }
}
